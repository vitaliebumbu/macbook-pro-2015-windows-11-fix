# Scripts

This folder intentionally does not include automatic driver installers.

The guide uses manual steps and built-in Windows commands so people can see what is being changed.

Apple Boot Camp driver files are not included because they belong to Apple.

